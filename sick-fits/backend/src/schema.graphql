# import * from './generated/prisma.graphql'

type Query {
    items: [Item]!
}

type Mutation {
    createItem(title: String, description: String, image: String,
                price: Int, largeImage: String): Item!
}